pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter({url "https://jcenter.bintray.com/"})
        mavenCentral()
    }
}
rootProject.name = "Metadjioo_DS"
include ':app'
